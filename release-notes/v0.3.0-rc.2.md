# Azoth v0.3.0-rc.2 — cohesive public knowledge surface

`v0.3.0-rc.2` is a release candidate for a cohesive, evidence-led public
surface around Azoth's intent-to-outcome engineering inquiry. It does not
retarget or alter the immutable `v0.3.0-rc.1` tag.

The candidate separates three kinds of public knowledge that were previously
compressed together: historical experience and evidence, an evolving working
thesis, and a narrow implemented proof. The README is the standalone hub that
routes readers among them.

## What changed

- Made the README a standalone 60-second explanation with visible branches to
  experience and evidence, the working thesis, executable proof, and source and
  architecture history.
- Rebuilt *Narrow Success, Broad Failure* around the causal lineage from
  production systems through the BigQuery migration, Local Requests Data
  bootloader reuse, project-local learning and promotion, composable roles and
  typed handoffs, framework overgrowth, and architectural contraction.
- Added *Intent-to-Outcome Engineering: A Working Thesis on Durable Agentic
  Systems*. It distinguishes model invocation, bounded work pulse, and durable
  agentic system; labels observations, engineering inferences, working
  hypotheses, and research horizons; and records counterarguments, falsifiable
  questions, and an open research agenda.
- Added reciprocal navigation among the case study, working thesis, and
  executable proof without using any document to expand another's claim.
- Demoted the working label `Personal Harness OS` from project identity to its
  accurate role: one compact implementation experiment whose existing paths are
  retained for provenance.
- Distinguished the broader outcome-graph direction from the routing, context,
  authority, and no-write rehearsal contracts implemented in this candidate.
- Made model evolution part of the architectural method: extra components and
  ceremony are falsifiable hypotheses that must earn and re-earn their place.
- Reframed the former Personal Harness document as an executable proof slice
  with a recruiter-readable claim map and explicit open questions.
- Added the thesis to fail-closed required paths, extraction assertions, and
  product-release smoke checks.
- Removed release-version drift from generated README and public-validator
  assets by deriving their versioned references from the extracted manifest.

## Implemented proof retained

- Effect-aware routing through `HarnessRequest`, `HarnessDecision`, and
  `RouteCapsule`.
- Compact, source-referenced context assembly with raw-memory filtering and
  explicit missing-source warnings.
- A portable four-case rehearsal surface that checks route, authority, warning,
  and repository no-write expectations.
- Thirty focused public tests for the selected routing, context, rehearsal, and
  personal-knowledge contracts.

## Claim boundary

The case study records historical experience and evidence. The thesis is a
qualified and revisable engineering model. The executable proof is the only
implemented public slice.

This candidate does not claim that the full intent-to-outcome system, autonomous
research-to-delivery pipeline, installer, cross-host parity, external adoption,
or production deployment of Azoth is complete. GloBuddy and SupplyOps are
production systems that shaped the method; they are not Azoth deployments.

## Validation evidence

The exact release-bearing extraction must pass:

- all 30 allowlisted portable public tests;
- the fail-closed public boundary validator;
- Ruff checks over the extracted public Python surface;
- privacy, credential-pattern, manifest, release-evidence, and local-reference
  validation; and
- a fresh no-write rehearsal against the extracted tree.

Exact source provenance is emitted into the extracted `azoth.yaml` manifest.
Publication remains gated on review of the complete extracted tree and explicit
human approval for the public commit, annotated tag, push, and prerelease.
