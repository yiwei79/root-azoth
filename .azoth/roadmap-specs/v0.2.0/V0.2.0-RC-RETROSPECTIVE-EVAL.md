# v0.2.0-rc.1 Retrospective Pipeline Eval

Date: 2026-04-29
Session: `2026-04-29-t-039-rc-retro-eval`
Pipeline command: `dynamic-full-auto`
Backlog task: `T-039`
Status: corrective retrospective complete; stable blocked

## Purpose

The `v0.2.0-rc.1` public prerelease was published after explicit operator
approval through the T-037 public handoff, but the publication step itself was
not originally executed through the full repo-native pipeline/eval envelope.

This retrospective run records the miss, evaluates the already-published RC,
and preserves the key boundary: this document is corrective evidence, not
retroactive proof that the original publication was properly staged.

## Operator Signal

RC approval:

```text
I accept the RC evidence and approve publishing v0.2.0-rc.1 through the T-037 public handoff. Do not publish stable yet.
```

Retrospective request:

```text
You didn't run the pipeline. I think we should do a retrospective run with the pipeline, with eval.
```

## Scope And Gates

- Retrospective scope: evaluate and document the RC publication process miss.
- P4 freeze: preserved; no new initiatives, autonomy expansion, intake
  decisions, governance mutation, or instruction mutation.
- Stable boundary: `v0.2.0` stable is not authorized and must not be published
  without a separate explicit operator approval.
- Scope gate: `2026-04-29-t-039-rc-retro-eval`, approved by human.
- Pipeline gate: `dynamic-full-auto`, approved for the same session.
- Write claim: acquired for the retrospective run while writing this evidence,
  then released before closeout.

## Stage 0 Classification

```yaml
scope: release-retrospective
risk: governance-touching
complexity: medium
knowledge: known-pattern
pipeline: dynamic-full-auto
eval_escalation:
  triggered: true
  triggers:
    - E1: multiple deliverables and acceptance branches to judge
    - E2: composed pipeline/evaluator boundary after governance-touching release work
    - E4: release and generated-surface blast radius
    - E6: explicit human request for pipeline eval
  threshold: 0.90
```

## Mechanical Checks In This Retrospective

Commands run from `/Users/yiwei/GithubRepos/root-azoth` on 2026-04-29:

| Command | Result |
| --- | --- |
| `python3 scripts/check_gates.py --session-id 2026-04-29-t-039-rc-retro-eval --require-pipeline-gate` | passed; scope and pipeline gates valid. |
| `python3 scripts/roadmap_dashboard.py` | passed; p4 shows T-034 through T-038 delivered and T-039 upcoming. |
| `python3 scripts/azoth-deploy.py --check` | passed; all 277 generated files in sync. |
| `python3 scripts/pipeline_lint.py` | passed. |
| `python3 scripts/azoth_extract_product.py --validate-only` | passed; `validate-only: OK (config, pipeline, templates)`. |
| `git diff --check` | passed. |
| `python3 scripts/product_release_smoke.py --out /private/tmp/azoth-retro-rc-smoke` | passed; bash consumer install passed; PowerShell skipped because `pwsh`/`powershell` is unavailable. |
| `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m pytest tests/test_autonomous_loop.py::test_strategy_preflight_includes_learning_harvester_gate tests/test_autonomous_loop.py::test_strategy_preflight_blocks_user_governed_learning_to_intake tests/test_autonomous_loop.py::test_strategy_preflight_requires_active_approved_campaign_for_auto_self_heal tests/test_autonomous_loop.py::test_campaign_report_exposes_learning_harvester_route_and_rejected_alternatives tests/test_autonomous_loop.py::test_completed_green_campaign_report_exposes_advisory_next_campaign_packet tests/test_autonomous_loop.py::test_campaign_report_observes_completed_handoff_without_continuing_current_loop tests/test_autonomous_loop.py::test_materialize_self_capture_writes_inbox_and_consumes_candidate tests/test_product_release_smoke.py -q` | passed; `10 passed in 1.14s`. |

Stable absence checks:

| Command | Result |
| --- | --- |
| `git -C /Users/yiwei/GithubRepos/azoth tag --list 'v0.2.0*'` | only `v0.2.0-rc.1` is present locally. |
| `git -C /Users/yiwei/GithubRepos/azoth show-ref --tags v0.2.0 v0.2.0-rc.1` | only `refs/tags/v0.2.0-rc.1` is present. |
| `gh release view v0.2.0 --repo yiwei79/azoth --json tagName,name,url,isPrerelease,targetCommitish` | `release not found`. |
| `gh release view v0.2.0-rc.1 --repo yiwei79/azoth --json tagName,name,url,isPrerelease,targetCommitish` | passed; prerelease URL is `https://github.com/yiwei79/azoth/releases/tag/v0.2.0-rc.1`. |

## Public RC State

- Source root commit for product extract: `f5dc6d3155ed521dd26b8d9921567d557ff3fe89`.
- Root branch evidence commit: `207f148`.
- Public commit: `0e93832ea5a9caff499128a84e4b046b8d44ac34`.
- Tag: `v0.2.0-rc.1`.
- Annotated tag object: `f7c7d6a9f0d54faf8d54ab1f22d43a34d5660f9e`.
- GitHub prerelease: `https://github.com/yiwei79/azoth/releases/tag/v0.2.0-rc.1`.

The local public checkout at `/Users/yiwei/GithubRepos/azoth` remains dirty and
behind `origin/main` from the first direct sync attempt. Publication was
completed through a clean temporary public worktree, so remote `origin/main` and
the RC release are correct. Local public checkout cleanup requires a separate
explicit destructive approval.

## Reviewer And Evaluator Results

Reviewer stage:

```yaml
stage_id: retro_s1_reviewer
agent: reviewer
disposition: approve
summary: preserve the already-published RC, record the process miss, keep stable blocked
```

Release evidence evaluator:

```yaml
stage_id: retro_s2_eval_release
agent: evaluator
threshold: 0.90
overall_score: 0.922
disposition: PASS
decision:
  rc_publication_evidence: sufficient_for_retrospective_reconciliation
  stable_v0_2_0_publication_authorized: false
```

Pipeline/governance evaluator:

```yaml
stage_id: retro_s2_eval_governance
agent: evaluator
threshold: 0.90
overall_score: 0.88
disposition: CONDITIONAL
required_follow_up:
  - add durable retrospective evidence distinguishing approved RC publication from original pipeline compliance
  - record stage summaries before closeout
  - resolve or explicitly carry lifecycle residue
  - keep stable blocked behind a separate human gate
```

Bounded correction applied in this run:

- Added this retrospective artifact.
- Added a post-publication retrospective note to
  `.azoth/roadmap-specs/v0.2.0/V0.2.0-RC-EVIDENCE-BUNDLE.md`.
- Added a T-039 evidence row linking this artifact.
- Recorded evaluator/reviewer stage evidence in the run ledger.
- Rechecked the bounded correction with a fresh evaluator; result `PASS` at
  `0.93`.

## Decision

Approved for RC preservation with a process-deviation note.

Do not roll back `v0.2.0-rc.1`: no reviewer or evaluator found a release-blocking
defect in the RC artifact itself. The defect was process compliance, and this
retrospective run records it directly.

Do not publish stable `v0.2.0`: stable remains blocked on a fresh explicit
operator approval, final stable preflight, no active write claim, and any
operator-required Windows/PowerShell installer proof.

## Stable Preflight Carry-Forward

Before any stable `v0.2.0` claim:

- Obtain separate explicit operator approval for stable.
- Re-run root validation and product smoke from the stable candidate source.
- Confirm no active run-ledger write claim.
- Verify `v0.2.0` tag and GitHub release absence before publishing.
- Decide whether PowerShell installer proof is required for stable.
- Confirm the public checkout cleanup state or use a clean temporary worktree.
- Preserve the supervised, campaign-bounded autonomy language in release notes.
