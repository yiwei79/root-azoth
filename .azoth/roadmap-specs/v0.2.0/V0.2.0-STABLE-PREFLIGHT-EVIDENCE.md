# v0.2.0 Stable Preflight Evidence

Date: 2026-04-29
Session: `2026-04-29-t-039-stable-preflight`
Pipeline command: `dynamic-full-auto`
Backlog task: `T-039`
Status: stable preflight complete; stable publication not approved

## Purpose

This run executes the concrete post-RC rollout steps after `v0.2.0-rc.1`:
soak/preflight validation, public release-state checks, learning/self-heal
contract proof, and evaluator review. It does not publish stable `v0.2.0`.

## Operator Constraints

- Stable publication is not approved in this run.
- Windows/PowerShell proof is explicitly skipped for this release by operator
  instruction.
- Local public checkout cleanup is not performed. Stable publication should use
  a clean temporary public worktree unless the operator separately approves
  destructive cleanup of `/Users/yiwei/GithubRepos/azoth`.

## Scope And Gates

- Scope gate: `2026-04-29-t-039-stable-preflight`, human approved.
- Pipeline gate: `dynamic-full-auto`, human approved.
- Write claim: acquired for evidence writes, released for full-suite and
  no-claim proof, reacquired only for evidence recording.
- P4 freeze: preserved. No new initiatives, broad autonomy expansion,
  automatic intake decisions, governance mutation, instruction mutation, or
  stable publication.

## Stage 0 Classification

```yaml
scope: stable-preflight
risk: release-governance
complexity: medium
knowledge: known-pattern
pipeline: dynamic-full-auto
windows_platform: skipped_by_operator_for_this_release
stable_publication: not_authorized
eval_escalation:
  triggered: true
  triggers:
    - E1: multiple acceptance branches to judge
    - E2: composed pipeline evaluator stage after release/governance work
    - E4: release and generated-surface blast radius
    - E6: explicit human request to use the pipeline correctly
  threshold: 0.90
```

## Root Validation

Commands run from `/Users/yiwei/GithubRepos/root-azoth`:

| Command | Result |
| --- | --- |
| `python3 scripts/check_gates.py --session-id 2026-04-29-t-039-stable-preflight --require-pipeline-gate` | passed; scope and pipeline gates valid. |
| `python3 scripts/roadmap_dashboard.py` | passed; p4 shows T-034 through T-038 delivered and T-039 upcoming. |
| `python3 scripts/azoth-deploy.py --check` | passed; all 277 generated files in sync. |
| `python3 scripts/pipeline_lint.py` | passed. |
| `python3 scripts/azoth_extract_product.py --validate-only` | passed; `validate-only: OK (config, pipeline, templates)`. |
| `git diff --check` | passed. |
| `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m pytest -q` | passed; `2000 passed, 1 xfailed, 2 xpassed in 62.69s`. |

## Product Validation

Command:

```bash
python3 scripts/product_release_smoke.py --out /private/tmp/azoth-stable-preflight-smoke
```

Result:

```text
product_release_smoke: OK
extract: /private/tmp/azoth-stable-preflight-smoke
consumer_bash: /var/folders/3y/6txc959n5gsbdty4w1_mkr7r0000gp/T/azoth-consumer-smoke-8au0tz_j
consumer_pwsh: skipped: pwsh/powershell not found
```

Interpretation:

- Product extraction and bash consumer smoke passed.
- Windows/PowerShell smoke is skipped for this release by operator instruction,
  so platform parity is not claimed.

## Learning And Self-Heal Evidence

Command:

```bash
python3 scripts/autonomous_loop.py campaign-report
```

Result:

```text
Autonomous-auto campaign report
Current loop: completed
Handoff: /Users/yiwei/GithubRepos/root-azoth/.azoth/handoffs/2026-04-25-autonomous-auto-development-handoff.md
Completion reason: vision_realized
Vision band: green -> target green
Fresh budget required: True
Safe to continue old campaign: False
Learning route: auto_self_heal_now
Learning rejected alternatives: capture_only, defer_to_intake, human_gate_required, proposal_needed, stale_or_rejected
Next campaign recommendation: advisory_only; fresh_approval=True; may_open_scope=False; top=none:none
```

No-claim operator-read proof after releasing the preflight write claim:

```text
Write claim: none
Completion reason: vision_realized
Next campaign recommendation: advisory_only; fresh_approval=True; may_open_scope=False; top=none:none
Residual risk: Campaign reached its completion condition; open a fresh budget to continue.
```

Focused tests:

```bash
PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m pytest \
  tests/test_autonomous_loop.py::test_strategy_preflight_includes_learning_harvester_gate \
  tests/test_autonomous_loop.py::test_strategy_preflight_blocks_user_governed_learning_to_intake \
  tests/test_autonomous_loop.py::test_strategy_preflight_requires_active_approved_campaign_for_auto_self_heal \
  tests/test_autonomous_loop.py::test_campaign_report_exposes_learning_harvester_route_and_rejected_alternatives \
  tests/test_autonomous_loop.py::test_completed_green_campaign_report_exposes_advisory_next_campaign_packet \
  tests/test_autonomous_loop.py::test_campaign_report_observes_completed_handoff_without_continuing_current_loop \
  tests/test_autonomous_loop.py::test_materialize_self_capture_writes_inbox_and_consumes_candidate \
  tests/test_product_release_smoke.py -q
```

Result: `10 passed in 1.12s`.

Coverage:

- Learning route and rejected alternatives are visible.
- Safe `auto_self_heal_now` still requires an active approved campaign.
- Completed Green campaigns require a fresh budget and cannot silently continue.
- Protected/user-governed/cross-system routes remain blocked to human gate or
  inbox/intake.
- No release evidence treats inbox rows as silently drained.

## Public Release State

Local public checkout checks:

| Command | Result |
| --- | --- |
| `git -C /Users/yiwei/GithubRepos/azoth tag --list 'v0.2.0*'` | only `v0.2.0-rc.1`. |
| `git -C /Users/yiwei/GithubRepos/azoth show-ref --tags v0.2.0 v0.2.0-rc.1` | only `refs/tags/v0.2.0-rc.1`. |
| `git -C /Users/yiwei/GithubRepos/azoth rev-parse --verify origin/main` | `0e93832ea5a9caff499128a84e4b046b8d44ac34`. |

Remote checks:

| Command | Result |
| --- | --- |
| `git -C /Users/yiwei/GithubRepos/azoth ls-remote --heads --tags origin main v0.2.0 v0.2.0-rc.1` | `origin/main` at `0e93832ea5a9caff499128a84e4b046b8d44ac34`; tag `v0.2.0-rc.1` at `f7c7d6a9f0d54faf8d54ab1f22d43a34d5660f9e`; no `v0.2.0` tag. |
| `gh release view v0.2.0 --repo yiwei79/azoth --json tagName,name,url,isPrerelease,targetCommitish` | `release not found`. |
| `gh release view v0.2.0-rc.1 --repo yiwei79/azoth --json tagName,name,url,isPrerelease,targetCommitish` | confirmed prerelease: `https://github.com/yiwei79/azoth/releases/tag/v0.2.0-rc.1`. |

## Reviewer And Evaluator Gate

Initial reviewer:

```yaml
stage_id: stable_s2_reviewer
disposition: request-changes
summary: validation body strong, but evidence doc, no-claim proof, and run closeout were not yet complete
```

Initial evaluator:

```yaml
stage_id: stable_s2_evaluator
threshold: 0.90
overall_score: 0.89
disposition: CONDITIONAL
summary: validation body strong; process compliance incomplete until evidence and no-claim/closeout proof are recorded
```

Bounded correction in this run:

- Recorded this stable-preflight evidence artifact.
- Added a T-039 evidence row linking this artifact.
- Recorded no-write-claim proof after releasing the write claim.
- Kept stable publication blocked behind a separate explicit human approval.

Final recheck:

```yaml
stage_id: stable_s2_recheck
threshold: 0.90
overall_score: 0.94
disposition: PASS
stable_publication_authorized: false
may_ask_operator_for_explicit_stable_approval: true_after_closeout
```

## Decision

Stable preflight validation is substantively green, with Windows/PowerShell
explicitly skipped by operator instruction. The final recheck found no blockers
to proceeding to a human stable-approval decision.

It must not publish stable `v0.2.0`. The next human-facing gate, after clean
closeout, is a separate explicit decision on whether to publish stable.
