# Azoth Toolkit Manifest
# This file tracks the toolkit's state and installed components.

name: root-azoth
version: 0.2.1.0
description: An inspectable toolkit for governed AI-assisted software delivery

# Root scaffold identity (D34-D38)
scope:
  mode: scaffold
  product_target: azoth
  is_development_workshop: true

# Milestone-local phase for the active working slice; welcome strip uses lifecycle_phase
phase: 1
milestone: v0.3.0
lifecycle_phase: 8  # historical 1–7 + Next marker for shipped pre-1.0 roadmap + v0.1.0 gate

# Architecture decisions count
decisions: 55

# Layer status
layers:
  molecule:  # Layer 0: Kernel
    status: complete
    files: 14  # 4 kernel + 4 templates + 6 adapters
  mineral:   # Layer 1: Skills
    status: complete
    skills: 17  # 5 extracted + 12 new
  wave:      # Layer 2: Agents
    status: active
    agents: 10  # 4 core + 2 research + 3 meta + 1 utility
  current:   # Layer 3: Pipelines
    status: active
    pipelines: 8  # 8 presets (P3-003 complete)

# Memory system
memory:
  episodes: 588
  patterns: 29
  last_sync: null

# Platform compatibility
platforms:
  claude_code: primary
  opencode: compatible  # Reads CLAUDE.md natively
  copilot: adapter      # Via .github/ files
  codex: adapter        # Via .codex/ + .agents/ files

# Sync source (set when configured)
sync:
  source: null
  last_sync: null
  sanitize_config: sync-config.yaml

# Pipeline presets
pipeline_presets:
  - full
  - deliver
  - hotfix
  - docs
  - research
  - review
  - refactor
  - auto  # default
