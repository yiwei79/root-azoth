# Development dependencies for Azoth (CLI UX, YAML, tests, lint).
# Install: pip install -r requirements-dev.txt
# Keep the release lint contract reproducible; upgrade Ruff deliberately.

rich>=13
pyyaml>=6
pytest>=8
ruff==0.15.9
