# Pipeline examples

Small YAML fixtures referenced by tests and docs. Each example should validate against its schema (where a validator script exists).

| Example | Schema | Validator |
|---------|--------|-----------|
| `architecture-proposal.minimal.yaml` | `pipelines/architecture-proposal.schema.yaml` | `scripts/architecture_proposal_validate.py` |
