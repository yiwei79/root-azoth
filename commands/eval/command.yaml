schema_version: 1
name: eval
display_name: /eval
description: Governance quality gate — evaluate artifacts; auto-escalates to swarm eval when warranted
agent: orchestrator
azoth_effect: read

body:
  mode: legacy_claude_markdown
  source_path: .claude/commands/eval.md

projection:
  claude:
    kind: native_command_markdown
    output_path: .claude/commands/eval.md
  cursor:
    kind: claude_command_toggle_surface
    output_path: .claude/commands/eval.md
  copilot:
    kind: prompt_markdown
    output_path: .github/prompts/eval.prompt.md
  opencode:
    kind: native_command_markdown
    output_path: .opencode/commands/eval.md
  codex:
    kind: skill_wrapper
    output_path: .agents/skills/azoth-eval/SKILL.md
    metadata_path: .agents/skills/azoth-eval/agents/openai.yaml
  gemini:
    kind: native_command_toml
    output_path: .gemini/commands/eval.toml
  antigravity:
    kind: workflow_markdown
    output_path: .agents/workflows/eval.md

migration:
  phase: canonical_body_batch_2_session_a
  runtime_source_of_truth: commands/eval/command.yaml
  notes:
    - Session A of T-003 moves /eval onto the neutral command path.
    - .claude/commands/eval.md remains the authored semantic body during the bridge.

references:
  - .azoth/roadmap-specs/v0.2.0/T-003.yaml
  - docs/CANONICAL_COMMAND_CONTRACT.md
