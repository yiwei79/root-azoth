schema_version: 1
name: sync
display_name: /sync
description: Extract patterns from a source framework into Azoth
agent: orchestrator
azoth_effect: write

body:
  mode: legacy_claude_markdown
  source_path: .claude/commands/sync.md

projection:
  claude:
    kind: native_command_markdown
    output_path: .claude/commands/sync.md
  cursor:
    kind: claude_command_toggle_surface
    output_path: .claude/commands/sync.md
  copilot:
    kind: prompt_markdown
    output_path: .github/prompts/sync.prompt.md
  opencode:
    kind: native_command_markdown
    output_path: .opencode/commands/sync.md
  codex:
    kind: skill_wrapper
    output_path: .agents/skills/azoth-sync/SKILL.md
    metadata_path: .agents/skills/azoth-sync/agents/openai.yaml
  gemini:
    kind: native_command_toml
    output_path: .gemini/commands/sync.toml
  antigravity:
    kind: workflow_markdown
    output_path: .agents/workflows/sync.md

migration:
  phase: canonical_body_batch_3_initiative_exit
  runtime_source_of_truth: commands/sync/command.yaml
  notes:
    - T-004 moves /sync onto the neutral command path.
    - .claude/commands/sync.md remains the authored semantic body during the bridge.

references:
  - .azoth/roadmap-specs/v0.2.0/T-004.yaml
  - docs/CANONICAL_COMMAND_CONTRACT.md
